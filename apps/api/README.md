# Creative Marketer API

FastAPI delivery layer and Python composition root for the Creative Marketer modular monolith. Reusable domain/application code is isolated in the sibling `creative_marketer` package. TASK-002 adds PostgreSQL tenancy through an async use-case-scoped unit of work; see the repository `DEVELOPMENT.md` for migrations and security tests.
